package algorithms;

/**
 * Represents a generic 1-D point with coordinates(x,y)
 */
public class Point {
    public double x;
    public double y;

    Point(double x, double y) {
        this.x = x;
        this.y = y;
    }
}
